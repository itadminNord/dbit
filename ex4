version: "1"

volumes:
    dbit_bd:

services:
  nginx:
    image: идентификатор пользователя/имя образа
     restart: always
     environment:
		- TZ=
		- MONGO_DB_ADDR=mongodb
         - MONGODB_PORT=27071

   mongodb:
     image: mongo:latest
     volumes:
       -mongodb_volume:/data/db
     restart: always


   web_service:
       build: WebService/
       restart:always
	   ports:
          - 8080:8080
       environment:
            - MONGO_DB_ADDR=mongodb
            - MONGODB_PORT=27071